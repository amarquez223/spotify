<div class="container">
	<div class="row">
			<% @artists.each do |artist| %>
				<div class="col-sm-4">
							<img class="img-responsive" src="<%= artist.image_url %>" width= 280 height= 172>
							<h2>
								<%= artist.name %>
							</h2>
							<%= link_to "Ver más", artist_path(artist.id), class: "btn btn-primary" %>
				</div>
			<% end %>
	</div>
</div>